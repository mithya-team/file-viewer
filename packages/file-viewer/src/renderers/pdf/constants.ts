/** Gap between stacked PDF page slots (px). Override via `--file-viewer-page-gap`. */
export const PAGE_GAP = 12;

/** Vertical prefetch margin for lazy page render (px). */
export const OBSERVER_MARGIN = 600;

/** Suppress scroll-driven page callbacks after programmatic scroll (ms). */
export const PROGRAMMATIC_SCROLL_GUARD_MS = 800;

/** Debounce before PDF text search scan (ms). */
export const FILE_VIEWER_SEARCH_DEBOUNCE_MS = 300;
