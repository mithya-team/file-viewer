/** Scrollport inside FileViewer's `relative min-h-0 flex-1 overflow-hidden` content slot. */
export const RENDERER_VIEWPORT_CLASS = "absolute inset-0 overflow-auto";

/** Centered status/loading inside the same content slot. */
export const RENDERER_VIEWPORT_CENTERED_CLASS =
  "absolute inset-0 flex items-center justify-center";
